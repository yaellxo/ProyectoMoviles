<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo del LinearLayout -->
    <solid android:color="#FFFFFF" /> <!-- Cambia el color de fondo según tus necesidades -->

    <!-- Bordes del LinearLayout -->
    <stroke
        android:width="2dp"
        android:color="#000000" />

    <!-- Esquinas redondeadas (opcional) -->
    <corners android:radius="8dp" /> <!-- Cambia el radio si no quieres esquinas redondeadas -->
</shape>
